# =============================================================================
# Mindspace — Root Requirements
# Full dependency list for the entire project (ML pipeline + deployment API).
# All versions pinned to what is installed in myenv (verified via pip freeze).
# =============================================================================

# ── ML Pipeline (full-final-pipeline.ipynb) ──────────────────────────────────
numpy==2.4.2
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0
xgboost==3.2.0
lightgbm==4.6.0
joblib==1.5.3
optuna==4.7.0
optuna-integration==4.7.0
tqdm==4.67.3
statsmodels==0.14.6

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib==3.10.8
seaborn==0.13.2
plotly==6.6.0

# ── Deployment APIs (deployment/ — FastAPI inference servers) ──────────────────
fastapi==0.134.0
uvicorn[standard]==0.41.0
pydantic==2.12.5
python-multipart==0.0.22

# ── Test Application (test-app/ — Flask frontend) ───────────────────────────────
Flask==3.1.3
requests==2.32.3

# ── Utilities (shared) ───────────────────────────────────────────────────────────
python-dotenv==1.2.1
